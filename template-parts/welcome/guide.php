<!-- Setup Guide -->
<div class="mb-12 rounded-2xl bg-white p-8 shadow-lg">
    <h2 class="mb-2 text-2xl font-bold text-gray-800">Setup Script</h2>
    <p class="mb-6 text-gray-600">Run <code class="rounded bg-gray-100 px-2 py-0.5 text-sm text-gray-700">npm run setup</code> to personalize this starter theme. The interactive script will:</p>

    <div class="space-y-4">
        <div class="flex gap-3">
            <div class="mt-0.5 flex h-6 w-6 flex-shrink-0 items-center justify-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">1</div>
            <div>
                <p class="text-gray-700"><span class="font-semibold">Collect your theme info</span> &mdash; name, description, author, and author URL. A text domain and slug are derived automatically from the name in kebab-case.</p>
            </div>
        </div>
        <div class="flex gap-3">
            <div class="mt-0.5 flex h-6 w-6 flex-shrink-0 items-center justify-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">2</div>
            <div>
                <p class="text-gray-700"><span class="font-semibold">Apply replacements across the codebase</span> &mdash; updates <code class="text-sm text-gray-600">style.css</code> header, <code class="text-sm text-gray-600">package.json</code> name, <code class="text-sm text-gray-600">README.md</code> title, welcome page heading, and asset handle prefixes in <code class="text-sm text-gray-600">inc/assets.php</code>.</p>
            </div>
        </div>
        <div class="flex gap-3">
            <div class="mt-0.5 flex h-6 w-6 flex-shrink-0 items-center justify-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">3</div>
            <div>
                <p class="text-gray-700"><span class="font-semibold">Swap the default font</span> &mdash; optionally replaces Roboto with any <a href="https://fontsource.org" class="text-blue-600 underline" target="_blank">fontsource</a> font. Uninstalls the old package, installs the new one, and updates the import and <code class="text-sm text-gray-600">font-family</code> rule in <code class="text-sm text-gray-600">fonts.css</code>.</p>
            </div>
        </div>
        <div class="flex gap-3">
            <div class="mt-0.5 flex h-6 w-6 flex-shrink-0 items-center justify-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">4</div>
            <div>
                <p class="text-gray-700"><span class="font-semibold">Remove the welcome page</span> &mdash; optionally deletes this demo page and resets the index template to a blank starting point.</p>
            </div>
        </div>
        <div class="flex gap-3">
            <div class="mt-0.5 flex h-6 w-6 flex-shrink-0 items-center justify-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">5</div>
            <div>
                <p class="text-gray-700"><span class="font-semibold">Clean up after itself</span> &mdash; optionally deletes <code class="text-sm text-gray-600">setup.js</code> and removes the <code class="text-sm text-gray-600">setup</code> script from <code class="text-sm text-gray-600">package.json</code>.</p>
            </div>
        </div>
    </div>
</div>
